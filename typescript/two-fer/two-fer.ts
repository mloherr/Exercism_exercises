/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(namePerson?: string): string {
  if (namePerson) {
    return `One for ${namePerson}, one for me.`;
  } else {
    return "One for you, one for me.";
  }
}
